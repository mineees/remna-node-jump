services:
  remnanode:
    image: remnawave/node:latest
    restart: unless-stopped
    hostname: remnanode
    container_name: remnanode
    network_mode: host
    env_file: ./remnanode/.env
    ulimits:
      nofile:
        soft: 1048576
        hard: 1048576
